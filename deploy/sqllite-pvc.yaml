---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sqllite-pvc
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    limits:
      storage: 100M
    requests:
      storage: 100M

